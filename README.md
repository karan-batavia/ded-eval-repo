# ded-eval-repo

Source files that exercise the DED classifier prompt against the cases in PROD-264, PROD-276 and PROD-278.

See `EXPECTED.md` for the per-variable expected result and the rule that decides each one.

Scan this repo, then diff the classifier output against `EXPECTED.md`.
